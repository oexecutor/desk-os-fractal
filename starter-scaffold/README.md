# Starter scaffold

Estes arquivos são orientação de bootstrap, não lockfile final. Claude Code deve resolver versões atuais e estáveis em documentação oficial, substituir placeholders, instalar dependências e versionar o lockfile. Não copiar schemas para múltiplos locais sem uma estratégia de geração/symlink definida.
