{
  "name": "@desk-os/approval",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Plan review/approve/activate transactional workflow.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@desk-os/domain": "workspace:*",
    "@desk-os/schemas": "workspace:*",
    "@desk-os/events": "workspace:*",
    "@desk-os/storage": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "@types/node": "22.20.1"
  }
}
